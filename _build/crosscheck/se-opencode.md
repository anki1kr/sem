# Cross-check Report: se

## Checked files
- `frag\se-imp.html` (answer file, 12 questions + 1-mark list)
- `frag\se-u1.html` (Unit I source)
- `frag\se-u2.html` (Unit II source)

## Problems found

1. **iq4 | HIGH | "No feedback to the feasibility study."**
   The answer states that iterative waterfall has no feedback to the feasibility study. This exact claim does not appear in either `se-u1.html` or `se-u2.html`. The source describes iterative waterfall as adding feedback paths from every phase to the previous phases, but never mentions the feasibility study in this context.

## Questions checked
iq1, iq2, iq3, iq4, iq5, iq6, iq7, iq8, iq9, iq10, iq11, iq12, and all 25 one-mark questions (Q1–Q25).

Every other fact, number, name, date, mnemonic, figure reference, worked entry, table entry, and attribute in the answer file was verified against the two source files and matched exactly.
